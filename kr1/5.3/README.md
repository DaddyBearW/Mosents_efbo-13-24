# Задание 5.3

Запуск:

```bash
uvicorn main:app --reload
```

Особенности:

- кука хранит `user_id` и время последней активности;
- данные подписаны;
- сессия живёт 5 минут;
- при активности после 3 минут кука продлевается.
